{
  "name": "pteverse",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "netlify database migrations apply"
  },
  "dependencies": {
    "@google/genai": "^1.16.0",
    "@netlify/blobs": "^10.0.8",
    "@netlify/database": "^0.4.0",
    "@netlify/functions": "^4.2.6",
    "@netlify/identity": "^0.2.0",
    "drizzle-orm": "beta",
    "openai": "^4.104.0"
  },
  "devDependencies": {
    "drizzle-kit": "beta"
  }
}
